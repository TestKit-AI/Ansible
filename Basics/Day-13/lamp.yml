- name: Install packages
  gather_facts: false
  hosts: all
  become: yes
  tasks:
    - name: Packed installing
      package: 
        name: 
          - python3-libselinux
          - python3-libsemanage
          - firewalld
        state: installed

